node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Custom Components (Ignore one-offs, but track the registry and examples)
src/custom/*
!src/custom/index.ts
!src/custom/PromoSlide.tsx